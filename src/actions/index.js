import { createAction } from '@reduxjs/toolkit'

export const TOGGLE_FULLSCREEN_WINDOW = createAction('TOGGLE_FULLSCREEN_WINDOW')
export const SET_FULLSCREEN_WINDOW = createAction('SET_FULLSCREEN_WINDOW')
export const CLOSE_WINDOW = createAction('CLOSE_WINDOW')
